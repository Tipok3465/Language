integer <- gcd(integer a, integer b) {
    while (b) {
        a = a % b;
        swap(a, b);
    }
    return a;
}

Main {
    fasttype;
    integer tests = 1;
    a = (a+b)/2 + 1;
    string s = "AAA\n";
    integer x = 1.125;
    bashfhueawbf123koaf134 = 123;
    a = 1; // tests = input();
    for (integer a = b/+5; a <= b; --a) {
        string ans = "void";
        print(ans);
    }
    while (--tests  > 0) {
        // aaaaaaa
        solve();
    }
}
