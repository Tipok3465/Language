integer <- gcd(integer a, integer b) {
    array <- float arr;
    integer x = 0;
    integer vz = arr[4];
    while (b) {
        a = a % b;
    }
    switch (5) {
        case (42) print(4);
    }
    return a;
    for (integer a = -52; a > 0; ++a) {
        break;
    }
}

integer b;

void <- A() print(5);

Main {
    float fasttype;
    integer tests = -1-2-3;
    //integer a = (a+b)/2/ -1;
    string s = "AAA\n";
    integer x = 1.125;
    float F = 1.0;
    //a = 1; // tests = input();
    integer ans;
    for (integer a = b/5; a <= b; --a) {
        string ans = "void";
        string a;
        "print(ans)";
        if(-6) a=a;
    }
   array <- bool arr1, arr2;
   while (--tests  >= -b + 0, "aaaaa") {
        // aaaaaaa
        A();
   }
}